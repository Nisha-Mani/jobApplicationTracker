import React from "react";

const JobCard = ({ companyName, title, status }) => {
  return (
    <div>
      <img src="" alt="" />
      <h2>{companyName}</h2>
      <h3>{title}</h3>
    </div>
  );
};

export default JobCard;
